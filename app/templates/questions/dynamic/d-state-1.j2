{% extends "questions/base.j2" %}

{% block content %}
<li>
  <fieldset>
    <legend>{{ question }}</legend>

    <div>
      <input type="radio" id="yes" name="selection" value="yes" :required="submitAction === 'next' || submitAction == 'done'" />
      <label for="huey">Yes</label>
    </div>

    <div>
      <input type="radio" id="no" name="selection" value="no" />
      <label for="no">No</label>
    </div>
  </fieldset>
</li>
{% endblock %}

{% block button %}
{% if is_end %}
<button @click="submitAction = 'previous'" id="previous" type="submit">previous</button>
<a href='/confirmation'>
  <button @click="submitAction = 'done'" id="done" type="button">done</button>
</a>
{% else %}
<button @click="submitAction = 'previous'" id="previous" type="submit">previous</button>
<button @click="submitAction = 'next'" id="next" type="submit">next</button>
{% endif %}
{% endblock %}
